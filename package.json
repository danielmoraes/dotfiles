{
  "name": "dotfiles",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "pnpm -r check",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^1.13.6"
  }
}
