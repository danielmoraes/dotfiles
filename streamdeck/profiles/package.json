{
  "name": "streamdeck-profiles",
  "version": "0.1.0",
  "description": "Generates the Stream Deck + profile (3 pages) from the layout in this repo",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "apply": "node src/generate.ts",
    "check": "vp check",
    "lint": "vp lint",
    "fmt": "vp fmt",
    "typecheck": "tsc --noEmit",
    "test": "vp test --run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24"
  }
}
